name: CI/CD Pipeline Simulación

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

      - name: Construir imagen Docker
        run: docker build -t mi-app ./app

      - name: Simulación de Test
        run: echo "Todos los tests pasaron con éxito"
